# CLAUDE.md — settings-modbus

## Что это

Подпроект ONEPAP.24 — документация и конфигурация Modbus RTU протокола для связи с контроллером цитологического процессора.

**Родительский проект:** `[[../README|ONEPAP.24]]` — автоматизированный цитологический процессор (24 стекла, окраска по Папаниколау).

## Назначение

Этот каталог содержит **единственный источник истины** для Modbus-регистров и протокола обмена с PLC (ESP32 / Medtechnica). Здесь:

- Описана полная карта регистров (операционных и сервисных)
- Задокументированы алгоритмы взаимодействия с регистрами
- Определены коды команд, коды ошибок и их отображение на UI
- Описаны сценарии окраски и промывки

## Ключевые файлы

| Файл | Назначение |
|---|---|
| `Протокол_Modbus.md` | **Главная спецификация** — карта регистров, коды команд, алгоритмы |
| `../01_Проект/Документ_настроек_Modbus_ONE24.md` | Сервисная карта (регистры 0..34), рецепты настройки |

## Технический контекст

### Аппаратный стек
- **Контроллер:** ESP32-based плата Medtechnica
- **Интерфейс:** Modbus RTU через UART/USB (COM4)
- **Параметры:** 115200 бод, 8N1, slave_id=1
- **Исполнительные устройства:** шаговые двигатели (NEMA17 + TMC2209), соленоидные клапаны, вакуумный насос, пережимные клапаны
- **Датчики:** концевики (LJ8A3-2-Z/BX), тензодатчик (HX711), датчик жидкости, ультразвуковой датчик уровня

### Программный стек
- **Язык:** Go 1.22 (управляющее ПО)
- **UI:** Web/htmx SPA (Wails v2 + WebView2)
- **Конфигурация:** TOML (BurntSushi/toml)
- **Modbus-библиотека:** goburrow/modbus
- **Симуляция:** Test Control Module (tcm_v2) для отладки без железа

### Связанные каталоги
- `../11_Software/Main-v6/` — исходный код ПО управления
- `../11_Software/Main-v6/config/fsm/screens/` — FSM-конфиги экранов (исполняемый источник истины)
- `../09_архив/Wails-v2/labflow/config/` — архивные TOML-конфиги (base_config.toml, user_settings.toml)

## Карта регистров (быстрый доступ)

**Операционные регистры (read/write через Modbus RTU):**

| Регистр | Имя | R/W | Суть |
|---|---|---|---|
| 1 | ready_status | R/W | **Чтение:** готовность (0=готов, 1=занят). **Запись:** 111 = аварийный сброс, 222 = сохранить EEPROM |
| 5 | status_error | R | Код ошибки (см. таблицу кодов) |
| 6 | mcu_temp | R | Температура MCU (°C × 10) |
| 7 | ntc_temp | R | Температура NTC (°C × 10) |
| 8 | hx711_weight | R | Тензодатчик HX711 (raw) |
| 9 | cmd_target | W | Код команды (90–200) |
| 19 | selector_target | R/W | Выбор переключающего клапана (1 = первый, 2 = второй) |
| 20 | selector_hole | R/W | Номер отверстия (0 = Home, 1..14 = позиции реагентов) |
| 21 | selector_coord | R/W | Координата отверстия (шаги двигателя) |
| 22..34 | inputs[0..12] | R | Биты дискретных входов (концевики, датчики) |
| 37 | current_step | W | Номер шага (0..15: 1..11 = окраска, 12..15 = промывка) |
| 38 | exposure_time | R/W | Время экспозиции (с) |
| 39 | liquid_fill_volume | R/W | Объём налива (мл×10) |
| 40 | reagent_empty | R | Флаг окончания реагента |
| 41 | reagent_empty_delta | R/W | Порог детекции пустого реагента |
| 46 | zone_select | W | Выбор зоны обработки: 1=первая, 2=вторая, 3=обе |

## Правила работы с документацией

1. **Один факт — один документ.** Этот `CLAUDE.md` — текущий источник истины для Modbus-регистров (на replaced `Протокол_Modbus.md`, который находится в разработке).
2. **При расхождении** с FSM-конфигами (`screens/*.toml`) — FSM-конфиги авторитетны.
3. **При расхождении** с архивным `base_config.toml` — этот документ авторитетен.
4. **При расхождении** с кодом (`configurator/modbus/registers.go`) — код авторитетен.
5. **Язык:** русский (документация), английский (ключи TOML, имена регистров, код).

## Коды команд (запись в регистр 9)

| Код | Константа | Назначение |
|---|---|---|
| 90 | CmdCalibrateValve | Калибровка клапана |
| 100 | CmdCalibrateSel1 | Калибровка селектора 1 |
| 110 | CmdCalibrateSel2 | Калибровка селектора 2 |
| 111 | CmdEmergencyStop | Аварийный стоп (запись в рег. 1) |
| 120 | CmdZeroRotor | Хомирование ротора |
| 130 | CmdDrainIntermediate | Промежуточный слив |
| 140 | CmdLoadMaterial | Загрузка образцов |
| 160 | CmdSedimentation | Осаждение |
| 180 | CmdStainCycle | Цикл окраски (11 шагов) |
| 200 | CmdWashSystem | Промывка системы (4 шага) |
| 222 | CmdSaveEEPROM | Сохранить настройки в EEPROM (запись в рег. 1) |

## Коды ошибок (регистр 5)

| Код | Имя | Описание |
|---|---|---|
| 0x01 | ERR_SYSTEM | Системная ошибка |
| 0x04 | ERR_ROTOR | Ошибка ротора |
| 0x05 | ERR_PUMP | Ошибка помпы |
| 0x06 | ERR_MULTIPLEXER | Ошибка мультиплексора |
| 0x07 | ERR_SENSOR | Ошибка датчика |
| 0x08 | ERR_VALVE | Ошибка клапана |
| 0x09 | ERR_REAGENT_LOW | Реагент закончился |
| 0x0A | ERR_SAMPLE_PLACEMENT | Ошибка размещения образца |
| 0x0B | ERR_GLASS_PLACEMENT | Ошибка размещения стекла |
| 0x0C | ERR_TIMEOUT | Таймаут операции |
| 0x0D | ERR_WASTE_FULL | Бак отходов полон |
| 0x0E | ERR_ROTOR_SYNC | Синхронизация ротора |
| 0x0F | ERR_POWER | Ошибка питания |

## Протокол окраски (быстрая справка)

11 шагов Папаниколау (индексы 1..11): Спирт → Гематоксилин → Вода → Вода → Спирт → OG → Спирт → EA → Спирт → Спирт → Спирт

Промывка: 4 шага (индексы 12..15): хлорка ×2, спирт, вода — коды пишутся в регистр 37.

System Check: 4 шага (калибровка клапана → калибровка селектора 1 → хомирование ротора → слив).

## Разработка

### При изменении карты регистров
1. Обновить `configurator/modbus/registers.go` (константы)
2. Обновить этот `CLAUDE.md` (карта регистров выше)
3. Проверить соответствие FSM-конфигов (`screens/*.toml`)
4. Обновить `configurator.toml` при необходимости

### При добавлении новых регистров
1. Согласовать адрес с прошивкой контроллера (избегать конфликтов с сервисной картой 0..34)
2. Добавить константу в `registers.go` с указанием R/W в комментарии
3. Добавить в карту регистров этого документа
4. Написать алгоритм использования (операции, валидация, обработка ошибок)
